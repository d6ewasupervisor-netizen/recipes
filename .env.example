# Local Postgres (matches docker-compose.yml defaults)
DATABASE_URL=postgresql://recipes:recipes@localhost:5433/recipes

# Optional: gate write API routes (leave empty to disable in local dev)
APP_PASSPHRASE=
